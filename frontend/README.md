# フロントエンド

## パスエイリアス

`tsconfig.app.json` `tsconfig.json` `vite.config.ts` でパスエイリアス (@) を設定しているため相対パス (../../components/...) の代わりに '@/components/...' のように簡潔にインポートできる

## コンポーネント

### ナビゲーションバー

サイドバーのこと。
サイズをドラッグで変更するオプションはMantine標準機能にないため、非実装。
